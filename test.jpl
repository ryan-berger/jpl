
fn g(i : int) : int {
    assert (i == 0), "should be non zero"
    return 1
}

fn f(i : int) : int {
    let x = 1 + i
    let y = i + i
    let z = g(1)
    let a = true
    let b = false
    let c = if (a || b) && a then 0 else 3
    return x * y * z
}

fn h({x : int, y : int, {z : int}}) : int {
    let a = 2
    let b = 3
    return a + b + z
}

fn i(x : float4) : int {
    let z = sum[r : 10, s : 10] (r + s)
    return z
}

fn j(x[h, w] : int[,]) : int {
    return h + w + x[2, 3]
}

fn k() : int[] {
    let test = array[r : 10] 1
    return test
}

