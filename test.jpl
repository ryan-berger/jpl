
fn g(i : int) : int {
    return 1
}

fn f(i : int) : int {
    let x = 1 + i
    let y = i + i
    let z = g(1)
    let a = true
    let b = false
    let c = if (a || b) && a then 0 else 3
    return x * y * z
}

fn h({x : int, y : int, {z : int}}) : int {
    let a = 2
    let b = 3
    return a + b + z
}

fn i(x : float4) : int {
    let z = sum[r : 10, s : 10] g(r + s)
    return z
}
