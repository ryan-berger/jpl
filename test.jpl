
fn g(i : int) : int {
    return 1
}

fn f(i : int) : int {
    let x = 1 + i
    let y = i + i
    let z = g(1)
    let a = true
    let b = false
    let c = if (a || b) && a then 0 else 3
    assert a, "yeet"
    assert b, "otheryeet"
    return x * y * z
}

